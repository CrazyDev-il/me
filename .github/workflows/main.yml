name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install and configure dependencies
      run: |
        npm install --progress=false
        npm run-script build
        git config --global user.email "chandrasekhartiwari@gmail.com"
        git config --global user.name "CrazyDev-il"
        git remote set-url origin https://CrazyDev-il:6c0e8af60a40dc93059b9dc365f22a4b146ef866@github.com/CrazyDev-il/me.git
        npm run deploy
