name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install and configure dependencies
      run: |
        npm install --progress=false
        npm run-script build
        git config --global user.email "chandrasekhartiwari@gmail.com"
        git config --global user.name "CrazyDev-il"
        git remote add origin https://github.com/CrazyDev-il/test.git
        npm run deploy
